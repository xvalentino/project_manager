<% @boards.each do |board| %>
  <%= link_to board.title, board_path(board) %>
<% end %>

<%= form_for @board do |f| %>
  <%= f.label :title %>:
  <%= f.text_field :title %><br />
  <%= f.submit %>
<% end %>
